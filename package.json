{
  "name": "omp-developer-attention-status",
  "version": "1.0.3",
  "description": "Developer attention cost status line plugin for OMP.",
  "type": "module",
  "license": "MIT",
  "files": [
    "src",
    "dist",
    "spec",
    "README.md",
    "CHANGELOG.md",
    "LICENSE",
    "THIRD_PARTY_NOTICES.txt"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc --noEmit -p tsconfig.json && tsc -p tsconfig.build.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "keywords": [
    "omp",
    "oh-my-pi",
    "plugin",
    "statusline",
    "developer-cost",
    "developer-attention"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/klondikemarlen/omp-developer-attention-status.git"
  },
  "homepage": "https://github.com/klondikemarlen/omp-developer-attention-status#readme",
  "bugs": {
    "url": "https://github.com/klondikemarlen/omp-developer-attention-status/issues"
  },
  "omp": {
    "extensions": [
      "./dist/index.js"
    ],
    "settings": {
      "monthlySalary": {
        "type": "number",
        "default": 6500,
        "min": 1,
        "description": "Monthly salary used for the developer cost meter."
      },
      "hoursPerWeek": {
        "type": "number",
        "default": 40,
        "min": 1,
        "description": "Working hours per week used for the developer cost meter."
      },
      "weeksPerYear": {
        "type": "number",
        "default": 52,
        "min": 1,
        "description": "Working weeks per year used for the developer cost meter."
      },
      "activeWindowMinutes": {
        "type": "number",
        "default": 5,
        "min": 1,
        "description": "How long one prompt keeps the developer meter active."
      },
      "refreshIntervalSeconds": {
        "type": "number",
        "default": 15,
        "min": 1,
        "description": "How often the status text refreshes while the meter is active."
      },
      "label": {
        "type": "string",
        "default": "dev",
        "description": "Lowercase suffix shown in the status line, such as dev."
      }
    }
  },
  "dependencies": {
    "@oh-my-pi/pi-coding-agent": "^16.1.16",
    "big.js": "^7.0.1",
    "proper-lockfile": "^4.1.2"
  },
  "devDependencies": {
    "@types/big.js": "^7.0.0",
    "@types/node": "^24.0.0",
    "@types/proper-lockfile": "^4.1.4",
    "tsx": "^4.20.6",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=20.6.0"
  }
}
